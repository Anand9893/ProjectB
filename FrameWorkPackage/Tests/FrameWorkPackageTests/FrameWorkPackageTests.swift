import XCTest
@testable import FrameWorkPackage

final class FrameWorkPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FrameWorkPackage().text, "Hello, World!")
    }
}
